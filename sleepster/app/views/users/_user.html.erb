<% if action == 'show' %>
  <a href="<%= users_url %>">Users Index Link</a>
<% else %>
  <a href="<%= user_url(user.id) %>">User Show Page</a>
<% end %>

<ul>
    <li><%= user.email %></li>
    <form action="<%= users_url %>" method="GET">
        <input type="submit" value="all users">
    </form>
    <li>Bands</li>
        <ul>

        </ul>
</ul> 